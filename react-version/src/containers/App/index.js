import React, { Component } from 'react';
import SearchPage from '../SearchPage';

class App extends Component {
  render() {
    return (
      <div>
        <SearchPage />
      </div>
    );
  }
}

export default App;
